// import { fromJS } from 'immutable';
// import { selectBuildRoutineContainerDomain } from '../selectors';

describe('selectBuildRoutineContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
